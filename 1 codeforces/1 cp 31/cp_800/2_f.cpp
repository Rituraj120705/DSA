#include<bits/stdc++.h>
using namespace std;


int main () {
    int t;
    cin>>t;

    while(t--){
        int n,x;
        cin>>n,x;

        vector<int>a;
        int n = a.size();
        a.push_back(0);

        for(int i=0;i<n;i++){
            int point;
            cin>>point;
            a.push_back(point);
        }
        a.push_back(x);


        int max_dist = INT_MIN;
        
        for(int i = 1;i<n; i++){
            if(i==)
        }

    }
}